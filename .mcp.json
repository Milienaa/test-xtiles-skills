{
  "mcpServers": {
    "xtiles": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://mcp-stage.xtiles.app/mcp",
        "--header",
        "Authorization: Bearer ${XTILES_API_TOKEN}"
      ]
    }
  }
}
